Node * deleteHead(Node *head)
{
    //Your code here
    Node *temp=head;
    if(head->next!=NULL)
    {
        head=head->next;
        delete temp;
        return head;
    }
    else
    {
        delete temp;
        return NULL;
    }
}
